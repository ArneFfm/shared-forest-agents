[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-forest"
version = "0.1.0"
description = "Zero-dependency client for the Shared Forest API: read the forest, plant a tree, sponsor custom trees."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
license = "MIT"
keywords = ["shared-forest", "mcp", "agents", "sdk"]

[project.urls]
Homepage = "https://world.ghardenlab.com/developers"
Documentation = "https://world.ghardenlab.com/developers"
Repository = "https://github.com/ArneFfm/shared-forest-agents"
Issues = "https://github.com/ArneFfm/shared-forest-agents/issues"

[tool.setuptools.packages.find]
include = ["shared_forest*", "world_forest*"]
